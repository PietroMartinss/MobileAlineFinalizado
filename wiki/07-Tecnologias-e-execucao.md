# Tecnologias e execução

## Tecnologias
- Flutter
- Dart
- JSON
- Material Design
- url_launcher

## Execução
```bash
flutter pub get
flutter run
```

As pastas `assets/data/` e `assets/images/` já estão configuradas no `pubspec.yaml`.
